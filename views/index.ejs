<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/Styles/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900">
    <h1 class="text-4xl font-bold text-center block mt-2 text-zinc-500">Simple todo List</h1>
<form action="/create" method="post" class="p-10 text-center text-white">
<input type="text" class="bg-zinc-800 outline-none p-1 rounded" name="Title" placeholder="Type Something...">
<input type="submit" name="Add" value="Add Task" class=" bg-blue-500 p-1 rounded-lg">
</form>
<div class="bg-zinc-900 max-w-64 block m-auto">
    <% if (allUsers.length > 0) { %>
        <% allUsers.reverse().forEach((user) => { %>
            <div class="h-10 w-full text-white rounded bg-zinc-800 mb-2 p-2 flex justify-between" >
                <h3 ><%= user.name %></h3>
                <div>
                    <a href='user/<%=user._id%>' title="Edit Task">✏</a>
                    <a href='delete/<%=user._id%>' data-url="Delete" class="ml-2" title="Delete Task Task">🗑</a>
                </div>
            </div>
        <% }) %>
    <% } else { %>
        <li class="h-10 w-full text-white rounded bg-zinc-800">No users found</li>
    <% } %>
</ul>

</body>
</html>